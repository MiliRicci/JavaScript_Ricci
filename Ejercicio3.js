// Ej3 - JavaScript

let string1 = prompt("Ingresa el primer string:");

let string2 = prompt("Ingresa el segundo string:");

// Comparar si son iguales
let igual = (string1 === string2);

// Comparar longitud 
let long = (string1.length === string2.length);

console.log("¿Los strings son iguales?: " + igual);
console.log("¿Los strings tienen la misma longitud?: " + long);
